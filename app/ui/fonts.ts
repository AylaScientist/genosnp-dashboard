import { Actor } from 'next/font/google';
import { Agdasima } from 'next/font/google';

export const actor = Actor({ weight: ["400"], subsets: ['latin'] });
export const agdasima = Agdasima({ weight: ["400"], subsets: ['latin'] });